spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations:
            - classpath:config/telemetry/{application}

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/